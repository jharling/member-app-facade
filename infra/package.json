{
  "name": "member-app-facade-infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "preview": "pulumi preview",
    "deploy": "pulumi up",
    "destroy": "pulumi destroy"
  },
  "dependencies": {
    "@pulumi/aws": "^7.28.0",
    "@pulumi/docker-build": "^0.0.17",
    "@pulumi/pulumi": "^3.223.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.0"
  }
}
